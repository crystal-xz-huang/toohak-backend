import { clear } from '../other';

import { 
    adminAuthRegister,
    adminUserDetails,
} from '../auth';

import {
    adminQuizCreate,
    adminQuizList,
    adminQuizInfo,  
} from '../quiz';

const ERROR = { error: expect.any(String) };

describe ('testing clear', () => {

});
